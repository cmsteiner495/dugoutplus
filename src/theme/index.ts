import { colors } from './colors';
import { spacing } from './spacing';
import { radius } from './radius';
import { typography } from './typography';
import { shadow } from './shadow';

export { colors, spacing, radius, typography, shadow };

export const theme = {
  colors,
  spacing,
  radius,
  typography,
  shadow,
};
